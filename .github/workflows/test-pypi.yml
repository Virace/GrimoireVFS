# 发布到 Test-PyPI 测试环境
# 
# 触发条件: 推送任何以 v 开头的 tag (如 v0.2.0, v0.2.0-dev1 等)
#
# 所有 tag 推送都会发布到 Test-PyPI，正式版需要手动创建 GitHub Release 才会发布到 PyPI
#
# 使用方法:
#   1. 修改 pyproject.toml 中的版本号
#   2. git commit -m "chore: bump version to x.x.x"
#   3. git tag vx.x.x
#   4. git push && git push --tags
#   → 自动测试并发布到 Test-PyPI

name: Publish to Test-PyPI

on:
  push:
    tags:
      - 'v*'  # 匹配所有 v 开头的 tag

permissions:
  contents: read

# 防止多个发布同时运行
concurrency:
  group: test-pypi-publish
  cancel-in-progress: false

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 安装 uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: 安装依赖
        run: uv sync --group dev

      - name: 运行测试
        run: uv run pytest -v

  publish:
    needs: test
    runs-on: ubuntu-latest
    environment:
      name: testpypi
      url: https://test.pypi.org/project/grimoirevfs/

    steps:
      - uses: actions/checkout@v4

      - name: 安装 uv
        uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true

      - name: 显示版本信息
        run: |
          TAG="${{ github.ref_name }}"
          VERSION=$(uv version --short)
          echo "📦 Tag: $TAG"
          echo "📦 Version: $VERSION"

      - name: 构建包
        run: uv build

      - name: 发布到 Test-PyPI
        env:
          UV_PUBLISH_TOKEN: ${{ secrets.TEST_PYPI_API_TOKEN }}
        run: uv publish --index testpypi
